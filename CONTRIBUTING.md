# Contributing

PRs welcome.

## Development

```bash
npm install
npm run dev -- --json       # run locally with tsx
npm run build               # build for production
npm test                    # run tests
npm run check               # build + run
```

## Release

Conventional commits on `develop`. Release-please handles versioning when merged to `main`.
